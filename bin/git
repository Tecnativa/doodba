#!/bin/bash
if [ "$1" == "clone" ] && [ -n "$GIT_AUTOSHARE_ENABLED" ] && command -v git-autoshare-clone >/dev/null 2>&1
then
    shift
    /usr/bin/git autoshare-clone "$@"
else
    /usr/bin/git "$@"
fi
