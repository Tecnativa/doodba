# Odoo is always required
./odoo:
    defaults:
        # Shallow repositores are faster & thinner
        depth: $DEPTH_DEFAULT
    remotes:
        odoo: https://github.com/odoo/odoo.git
    target:
        odoo $ODOO_VERSION
    merges:
        - odoo $ODOO_VERSION
